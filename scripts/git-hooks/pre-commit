#!/bin/sh
# Instalado por scripts/ensure-git-hooks.mjs (npm install / prepare)
# Rápido: lint + test + build-check (Vite minify off; no quality completo).
set -e
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT" || exit 1
npm run lint
npm run test
npm run build:check
